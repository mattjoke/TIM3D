import { Raycaster, Vector3 } from "three";
import Init from "../Init";

const Selector = (event: {
    preventDefault: () => void;
    clientX: number;
    clientY: number;
}) => {};

export default Selector;
